{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}

{% block title %}Kurs erstellen{% endblock %}

{% block body %}

    <div class="row">
        <h1>Kurs erstellen</h1>
        <p>Hier können Sie einen Kurs erstellen.</p>
        {{ form_start(form) }}
        {% if not form.vars.valid %}
            <div>{{ form_errors(form) }}</div>
        {% endif %}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>

{% endblock %}